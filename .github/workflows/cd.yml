name: CI Tests
on:
  push:
    branches:
      - develop

jobs:
  tests:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check modules
        run: yarn install --immutable --immutable-cache
